# 🎭 Fsociety Arsenal

> 30+ hacking tools – all running on **Termux non-root**

---

## ⚡ Instalasi
```bash
pkg update -y && pkg upgrade -y
pkg install git python nmap -y
git clone https://github.com/<username>/fsociety-arsenal.git
cd fsociety-arsenal
pip install -r requirements.txt
python fsociety.py
